export type Univs = {
  university: Univ[];
};

export type Univ = {
  universityId: number;
  name: string;
};

export type Regions = Region[];

export type Region = {
  regionId: number;
  name: string;
};
